version: '3.8'
services:
  web:
    build:
      context: .
      dockerfile: MinimalChatbot.Web/Dockerfile
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ConnectionStrings__MinimalChatbot: Data Source=/app/data/minimalchatbot.db
      LLM__Provider: ${LLM__Provider}
      LLM__LMStudio__Endpoint: ${LLM__LMStudio__Endpoint}
      LLM__LMStudio__ApiKey: ${LLM__LMStudio__ApiKey}
      LLM__LMStudio__Model: ${LLM__LMStudio__Model}
      LLM__OpenRouter__Endpoint: ${LLM__OpenRouter__Endpoint}
      LLM__OpenRouter__ApiKey: ${LLM__OpenRouter__ApiKey}
      LLM__OpenRouter__Model: ${LLM__OpenRouter__Model}
      LLM__NanoGPT__Endpoint: ${LLM__NanoGPT__Endpoint}
      LLM__NanoGPT__Model: ${LLM__NanoGPT__Model}
      MCP_ENDPOINT: http://mcp-gateway:8033/mcp
      MicrosoftLearnMcp__Endpoint: ${MicrosoftLearnMcp__Endpoint}
    volumes:
      - minimalchatbot-data:/app/data
    ports:
      - "5244:8080"
    depends_on:
      - mcp-gateway
  mcp-gateway:
    image: ghcr.io/modelcontextprotocol/gateway:latest
    command: [ "mcp", "gateway", "run", "--transport", "streaming", "--port", "8033", "--servers", "minimalchatbot" ]
    ports:
      - "8033:8033"
volumes:
  minimalchatbot-data:
